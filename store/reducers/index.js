// import { loadingReducer, loadingState } from "./loading.R";

// export const initialState = { loading: loadingState };
// const combineReducers = (...reducers) => (state, action) => {
//   for (let i = 0; i < reducers.length; i++) state = reducers[i](state, action);
//   return state;
// };
// const combinedReducer = combineReducers(loadingReducer);

// export default combinedReducer;
